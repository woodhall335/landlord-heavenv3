# Funnel report validation

The existing admin growth view now reports canonical stages from organic landing through delivered document. It supports date, source route, product, device, traffic source, experiment, and authenticated-state filters. It calculates offer CTR and all requested journey rates, plus revenue by source page and product. Queries return aggregate event data and no customer rows or personal fields.

Focused growth-report tests passed.
