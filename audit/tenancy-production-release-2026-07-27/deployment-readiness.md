# Deployment readiness

**Not ready.**

Local focused checks pass, but the mandatory full suite was stopped and production database,
Vercel, Stripe, webhook, storage, logs, real browser payments and document-output validation
are unavailable. The working tree is not clean and no release commit exists.

