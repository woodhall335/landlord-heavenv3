---
title: Eviction Case Summary
category: guidance
description: Overview of case facts and key details
filename: case_summary.pdf
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Eviction Case Summary</title>
  <style>
    {{{print_css}}}

    body {
      font-family: Arial, sans-serif;
      font-size: 11pt;
      line-height: 1.5;
      color: #333;
    }

    .header {
      text-align: center;
      margin-bottom: 2em;
      padding-bottom: 1em;
      border-bottom: 3px solid #1a1a1a;
    }

    .header h1 {
      font-size: 18pt;
      margin: 0 0 0.5em 0;
      text-transform: uppercase;
    }

    .header p {
      font-size: 10pt;
      color: #666;
      margin: 0;
    }

    .section {
      margin: 1.5em 0;
      padding: 1em;
      border: 1px solid #ddd;
      background: #fafafa;
    }

    .section h2 {
      font-size: 12pt;
      margin: 0 0 0.75em 0;
      padding-bottom: 0.5em;
      border-bottom: 2px solid #1a1a1a;
      text-transform: uppercase;
    }

    .detail-row {
      display: flex;
      margin: 0.5em 0;
    }

    .detail-label {
      font-weight: 600;
      width: 200px;
      flex-shrink: 0;
    }

    .detail-value {
      flex-grow: 1;
    }

    .highlight-box {
      background: #f0f0f0;
      padding: 1em;
      margin: 1em 0;
      border-left: 4px solid #1a1a1a;
    }

    .footer {
      margin-top: 2em;
      padding-top: 1em;
      border-top: 2px solid #1a1a1a;
      font-size: 9pt;
      color: #666;
    }
  </style>
</head>
<body>

<div class="header">
  <h1>Eviction Case Summary</h1>
  <p>Generated on {{current_date}}</p>
</div>

<div class="section">
  <h2>Property Details</h2>
  <div class="detail-row">
    <span class="detail-label">Property Address:</span>
    <span class="detail-value">{{property_address}}</span>
  </div>
  <div class="detail-row">
    <span class="detail-label">Jurisdiction:</span>
    <span class="detail-value">{{jurisdiction}}</span>
  </div>
</div>

<div class="section">
  <h2>Parties</h2>
  <div class="detail-row">
    <span class="detail-label">Landlord:</span>
    <span class="detail-value">{{landlord_full_name}}</span>
  </div>
  {{#if landlord_address}}
  <div class="detail-row">
    <span class="detail-label">Landlord Address:</span>
    <span class="detail-value">{{landlord_address}}</span>
  </div>
  {{/if}}
  <div class="detail-row">
    <span class="detail-label">Tenant:</span>
    <span class="detail-value">{{tenant_full_name}}</span>
  </div>
  {{#if tenant_2_name}}
  <div class="detail-row">
    <span class="detail-label">Second Tenant:</span>
    <span class="detail-value">{{tenant_2_name}}</span>
  </div>
  {{/if}}
</div>

<div class="section">
  <h2>Tenancy Details</h2>
  <div class="detail-row">
    <span class="detail-label">Tenancy Type:</span>
    <span class="detail-value">{{tenancy_type}}</span>
  </div>
  <div class="detail-row">
    <span class="detail-label">Start Date:</span>
    <span class="detail-value">{{tenancy_start_date}}</span>
  </div>
  {{#if fixed_term_end_date}}
  <div class="detail-row">
    <span class="detail-label">Fixed Term End:</span>
    <span class="detail-value">{{fixed_term_end_date}}</span>
  </div>
  {{/if}}
  <div class="detail-row">
    <span class="detail-label">Rent:</span>
    <span class="detail-value">£{{rent_amount}} {{rent_frequency}}</span>
  </div>
  {{#if deposit_amount}}
  <div class="detail-row">
    <span class="detail-label">Deposit:</span>
    <span class="detail-value">£{{deposit_amount}}</span>
  </div>
  <div class="detail-row">
    <span class="detail-label">Deposit Protected:</span>
    <span class="detail-value">{{#if deposit_protected}}Yes{{else}}Not confirmed{{/if}}</span>
  </div>
  {{/if}}
</div>

<div class="section">
  <h2>Eviction Details</h2>
  <div class="detail-row">
    <span class="detail-label">Eviction Route:</span>
    <span class="detail-value">{{eviction_route}}</span>
  </div>
  {{#if ground_numbers}}
  <div class="detail-row">
    <span class="detail-label">Grounds:</span>
    <span class="detail-value">{{ground_numbers}}</span>
  </div>
  {{/if}}
  {{#if total_arrears}}
  <div class="highlight-box">
    <strong>Rent Arrears:</strong> £{{total_arrears}}
  </div>
  {{/if}}
</div>

{{#if notice_served_date}}
<div class="section">
  <h2>Notice Details</h2>
  <div class="detail-row">
    <span class="detail-label">Notice Served:</span>
    <span class="detail-value">{{notice_served_date}}</span>
  </div>
  {{#if notice_expiry_date}}
  <div class="detail-row">
    <span class="detail-label">Notice Expires:</span>
    <span class="detail-value">{{notice_expiry_date}}</span>
  </div>
  {{/if}}
</div>
{{/if}}

<div class="footer">
  <p><strong>Landlord Heaven</strong> | Case Summary for Eviction Proceedings</p>
  <p><em>This is a summary of information provided. It is not legal advice. Always verify details before proceeding.</em></p>
</div>

</body>
</html>
