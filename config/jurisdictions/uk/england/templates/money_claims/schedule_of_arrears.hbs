{{!--
  ============================================================================
  SCHEDULE OF DEBT (Court-Ready)
  Money Claim Pack - England
  ============================================================================
  Court-credible schedule with:
  - Running balance column (for arrears)
  - FIFO payment allocation
  - Clear period-by-period breakdown
  - Dynamic title based on claim type
--}}

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{#if arrears_schedule}}Schedule of Rent Arrears{{else}}Schedule of Debt{{/if}}</title>
  <style>
    {{{print_css}}}

    /* ===== @PAGE OVERRIDE ===== */
    @page {
      size: A4;
      margin: 10mm;
    }

    body {
      padding: 0;
      margin: 0 auto;
      font-family: Arial, sans-serif;
      font-size: 10pt;
      line-height: 1.5;
      color: #1a1a1a;
    }

    /* Document Header */
    .doc-header {
      text-align: center;
      margin-bottom: 1.5em;
      padding-bottom: 1em;
      border-bottom: 2px solid #1a1a1a;
    }

    .doc-header h1 {
      font-size: 14pt;
      font-weight: 600;
      margin: 0 0 0.5em 0;
      line-height: 1.3;
      color: #1a1a1a;
    }

    .doc-badge {
      display: inline-block;
      background: #1a1a1a;
      color: white;
      padding: 0.3em 0.8em;
      font-size: 10pt;
      font-weight: 600;
      letter-spacing: 0.5px;
    }

    /* Case Details Box */
    .case-details {
      border: 2px solid #1a1a1a;
      padding: 1em;
      margin: 1.5em 0;
      background: #fafafa;
    }

    .case-details-grid {
      display: grid;
      grid-template-columns: 140px 1fr;
      gap: 0.4em 1em;
      font-size: 10pt;
    }

    .case-details-grid dt {
      font-weight: 600;
      color: #444;
    }

    .case-details-grid dd {
      margin: 0;
      color: #1a1a1a;
    }

    /* Summary Box */
    .summary-box {
      border: 2px solid #1a1a1a;
      padding: 1em;
      margin: 1.5em 0;
      background: #f0f9ff;
    }

    .summary-box h3 {
      margin: 0 0 0.8em 0;
      font-size: 11pt;
      font-weight: 600;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0.8em 2em;
    }

    .summary-item {
      padding: 0.6em;
      background: white;
      border: 1px solid #ddd;
    }

    .summary-item dt {
      font-size: 9pt;
      color: #666;
      margin-bottom: 0.2em;
    }

    .summary-item dd {
      margin: 0;
      font-size: 11pt;
      font-weight: 600;
    }

    /* Arrears Table */
    .arrears-table {
      width: 100%;
      border-collapse: collapse;
      margin: 1.5em 0;
      font-size: 9pt;
    }

    .arrears-table th,
    .arrears-table td {
      padding: 6px 8px;
      border: 1px solid #1a1a1a;
      text-align: left;
    }

    .arrears-table th {
      background: #1a1a1a;
      color: white;
      font-weight: 600;
      white-space: nowrap;
    }

    .arrears-table td {
      background: #fff;
    }

    .arrears-table tr:nth-child(even) td {
      background: #f9fafb;
    }

    .arrears-table .amount {
      text-align: right;
      font-family: 'Courier New', monospace;
    }

    .arrears-table .period-col {
      min-width: 180px;
    }

    .arrears-table .notes-col {
      font-size: 8pt;
      color: #666;
    }

    /* Total Row */
    .total-row {
      background: #1a1a1a !important;
      color: white;
      font-weight: 700;
    }

    .total-row td {
      background: #1a1a1a !important;
      color: white !important;
      border-color: #1a1a1a;
    }

    /* Info Box */
    .info-box {
      background: #f8f8f8;
      border-left: 4px solid #1a1a1a;
      padding: 1em;
      margin: 1.5em 0;
      font-size: 10pt;
    }

    /* Document Footer */
    .doc-footer {
      margin-top: 2em;
      padding-top: 1em;
      border-top: 2px solid #e5e7eb;
      font-size: 9pt;
      color: #6b7280;
    }

    .doc-footer p {
      margin: 0.3em 0;
    }

    @media print {
      .arrears-table {
        page-break-inside: avoid;
      }
    }
  </style>
</head>
<body>

<!-- Document Header -->
<div class="doc-header">
  <h1>{{#if arrears_schedule}}Schedule of Rent Arrears{{else}}Schedule of Debt{{/if}}</h1>
  <span class="doc-badge">Money Claim – County Court</span>
</div>

<!-- Case Details -->
<div class="case-details">
  <dl class="case-details-grid">
    <dt>Property:</dt>
    <dd>{{property_address}}</dd>
    <dt>Defendant:</dt>
    <dd>{{tenant_full_name}}{{#if tenant_2_name}}, {{tenant_2_name}}{{/if}}</dd>
    <dt>Claimant:</dt>
    <dd>{{landlord_full_name}}{{#if landlord_2_name}}, {{landlord_2_name}}{{/if}}</dd>
    {{#if claimant_reference}}
    <dt>Claim Reference:</dt>
    <dd>{{claimant_reference}}</dd>
    {{/if}}
    <dt>Schedule Date:</dt>
    <dd>{{format_date generation_date "long"}}</dd>
  </dl>
</div>

<!-- Arrears Summary -->
<div class="summary-box">
  <h3>Arrears Summary</h3>
  <div class="summary-grid">
    <div class="summary-item">
      <dt>Monthly Rent</dt>
      <dd>{{currency rent_amount}}</dd>
    </div>
    <div class="summary-item">
      <dt>Payment Frequency</dt>
      <dd>{{rent_frequency}}</dd>
    </div>
    <div class="summary-item">
      <dt>Tenancy Start Date</dt>
      <dd>{{format_date tenancy_start_date "long"}}</dd>
    </div>
    <div class="summary-item">
      <dt>Total Arrears Claimed</dt>
      <dd><strong>{{currency arrears_total}}</strong></dd>
    </div>
  </div>
</div>

<!-- Arrears Schedule Table with Running Balance -->
<table class="arrears-table">
  <thead>
    <tr>
      <th class="period-col">Period</th>
      <th>Due Date</th>
      <th class="amount">Rent Due</th>
      <th class="amount">Paid</th>
      <th class="amount">Arrears</th>
      <th class="amount">Running Balance</th>
      <th class="notes-col">Notes</th>
    </tr>
  </thead>
  <tbody>
    {{#each arrears_schedule}}
    <tr>
      <td class="period-col">{{period}}</td>
      <td>{{format_date due_date "long"}}</td>
      <td class="amount">{{currency amount_due}}</td>
      <td class="amount">{{currency amount_paid}}</td>
      <td class="amount">{{currency arrears}}</td>
      <td class="amount"><strong>{{currency running_balance}}</strong></td>
      <td class="notes-col">{{#if notes}}{{notes}}{{/if}}</td>
    </tr>
    {{/each}}
    <tr class="total-row">
      <td colspan="4"><strong>Total Arrears Outstanding</strong></td>
      <td class="amount"><strong>{{currency arrears_total}}</strong></td>
      <td class="amount"><strong>{{currency arrears_total}}</strong></td>
      <td></td>
    </tr>
  </tbody>
</table>

<!-- Payment Allocation Note -->
<div class="info-box">
  <strong>Payment Allocation Method:</strong> Payments have been allocated using the FIFO (First In, First Out) method, applying payments to the oldest outstanding rent periods first. This is the standard court-accepted method for arrears allocation.
</div>

<!-- Evidence Note -->
<div class="info-box">
  <strong>Evidence Requirements:</strong> Attach proof of payments received, bank statements, and rent ledgers behind this schedule. The County Court expects a clear period-by-period breakdown matching the tenancy rent frequency.
</div>

<!-- Footer -->
<div class="doc-footer">
  <p><strong>Generated:</strong> {{format_date generation_date "long"}}</p>
  <p><strong>Landlord Heaven Money Claim Pack</strong> | England</p>
  <p><em>This schedule supports the particulars of claim and demonstrates the calculation of arrears owed.</em></p>
</div>

</body>
</html>
