/**
 * Review values must come from a verified review source. Until that source is
 * connected, the UI and structured data deliberately avoid numeric claims.
 */
export const VERIFIED_REVIEW_SUMMARY = null;
